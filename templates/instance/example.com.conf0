server {
    listen URL:80;
    server_name URL;
    add_header Access-Control-Allow-Origin *;

    location / {
        root WEBDIR/URL;
        index index.html;
    }

    location /data/ {
        proxy_pass http://127.0.0.1:4PORT/;
    }
}
